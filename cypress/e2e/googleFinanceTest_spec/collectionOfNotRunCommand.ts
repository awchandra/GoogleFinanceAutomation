// Step 4: Compare the highest value by clicking on the highest point of the YTD chart
        //googleFinancePage.clickHighestYTDPoint()

        // Step 6: Go to the highest tooltip from the chart to compare values
        //googleFinancePage.clickHighestTooltipComparison()
        // Step 7: Compare the content values
        //cy.log('Stock of ' + this.data.stock + 'is ' + firstStockValue)
        //cy.log('Stock of the Second Stock is ' + secondStockValue)

        // Step 10: Paste the copied link and verify the result
        //googleFinancePage.fillSearchStocks('{ctrl+v}{enter}')
        //googleFinancePage.verifyCopyPasteResult()